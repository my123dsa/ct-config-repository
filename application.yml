spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/my123dsa/ct-config-repository
          default-label: main
          timeout: 5



server:
  port: 8888
